const fib := fn (n : int) -> int => return (n < 2) ? n : (fib(n - 1) + fib(n - 2));

const main := fn => {
	for i in 0..=30 => {
		print(fib(i));
	}
};

@bench test "fib" => main();

test "fib_10" => assert(fib(10) = 55, "Fib 10 did not equal 55 as expected");