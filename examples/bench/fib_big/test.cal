const fib := fn (n : big) -> big => return (n < 2g) ? n : (fib(n - 1g) + fib(n - 2g));

const main := fn => {
	let n := 28g;
	let result := fib(n);
	print(result);
};

@bench test "fib" => main();

test "fib_10" => assert(fib(10) = 55, "Fib 10 did not equal 55 as expected");