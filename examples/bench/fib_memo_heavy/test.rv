let memo = {}

fn fib(n) do
	let cached = memo[n]
    if cached != :undef do
        return cached
    end
    
    let result = if n < 2 n
    else fib(n - 1) + fib(n - 2)
    
    memo[n] = result
    result
end

for i in 0..91 do
    print(fib(i))
end