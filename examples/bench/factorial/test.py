def factorial(x):
    if x <= 1:
        return 1
    return x * factorial(x - 1)

if __name__ == "__main__":
    n = 20
    result = factorial(n)
    result = factorial(n)
    result = factorial(n)
    result = factorial(n)
    result = factorial(n)    
    print(result)
