import (Channel, Mutex, WaitGroup) from std::async;

const main := fn => {
	let mut ch := Channel:<str>.new();
	
	let wg := spawn => {
		emit ch "Hello, Joe!";
	};
	
	defer wg.wait();
	
	if let .Some : x <- ch.get() => print(x);
};
