const sha256_hex := fn (input : str) -> str => return sha256(input);
const sha512_hex := fn (input : str) -> str => return sha512(input);
const blake3_hex := fn (input : str) -> str => return blake3(input);