impl<E, T> result:<E, T> {
	const is_ok := fn (self : E!T) -> bool => match self {
		.Ok : _ => return true,
		.Err : _ => return false
	};

	const is_err := fn (self : E!T) -> bool => match self {
		.Ok : _ => return false,
		.Err : _ => return true
	};

	const unwrap := fn (self : E!T) -> T => match self {
		.Ok : v => return v,
		.Err : _ => panic("unwrap on err")
	};

	const map := fn <U> (self : E!T, op : fn (T) -> U) -> U!E => match self {
		.Ok : v => return ok(op(v)),
		.Err : e => return err(e)
	};

	const map_err := fn <F> (self : E!T, op : fn (E) -> F) -> T!F => match self {
		.Ok : v => return ok(v),
		.Err : e => return err(op(e))
	};
};