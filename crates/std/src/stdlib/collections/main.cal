type HashMap:<K, V> := struct { } @overload {

	const "[]" := fn (self : &HashMap:<K, V>, key : K) -> V? => return self.get(key);

	const "[]=" := fn (self : &mut HashMap:<K, V>, key : K, value : V) => {
		self.set(key, value);
	};

	const "in" := fn (key : K, map : HashMap:<K, V>) -> bool => return map.contains(key);
};

type HashSet:<T> := struct { } @overload {
	const "in" := fn (value : T, set : HashSet:<T>) -> bool => return set.contains(value);
};

type HashMapIter:<K, V> := struct { data : list:<<K, V>>, index : int };

type HashSetIter:<T> := struct { data : list:<T>, index : int };

impl<K, V> HashMap:<K, V> {
	const new := fn (entries : list:<<K, V>>) -> HashMap:<K, V> => return hashmap_new(entries);

	const set := fn (self : &mut HashMap:<K, V>, key : K, value : V) => hashmap_set(self, key, value);

	const get := fn (self : &HashMap:<K, V>, key : K) -> V? => return hashmap_get(self, key);

	const remove := fn (self : &mut HashMap:<K, V>, key : K) -> V? => return hashmap_remove(self, key);

	const contains := fn (self : &HashMap:<K, V>, key : K) -> bool => return hashmap_contains(self, key);

	const len := fn (self : &HashMap:<K, V>) -> int => return hashmap_len(self);

	const is_empty := fn (self : &HashMap:<K, V>) -> bool => return self.len() = 0;

	const keys := fn (self : &HashMap:<K, V>) -> list:<K> => return hashmap_keys(self);

	const values := fn (self : &HashMap:<K, V>) -> list:<V> => return hashmap_values(self);

	const entries := fn (self : &HashMap:<K, V>) -> list:<<K, V>> => return hashmap_entries(self);

	const clear := fn (self : &mut HashMap:<K, V>) => hashmap_clear(self);

	const set_all := fn (self : &mut HashMap:<K, V>, entries : list:<<K, V>>) => {
		for i in 0..len(entries) => {
			let pair := entries[i];
			self.set(pair.0, pair.1);
		};
	};

	const get_or := fn (self : &HashMap:<K, V>, key : K, default : V) -> V => {
		match self.get(key) {
			.Some : v => return v,
			.None => return default
		};
	};

	const iter := fn (self : &HashMap:<K, V>) -> HashMapIter:<K, V> => return HashMapIter:<K, V> { data : self.entries(), index : 0 };

	const iter_mut := fn (self : &mut HashMap:<K, V>) -> HashMapIter:<K, V> => return HashMapIter:<K, V> { data : self.entries(), index : 0 };

	const into_iter := fn (self : HashMap:<K, V>) -> HashMapIter:<K, V> => return HashMapIter:<K, V> { data : self.entries(), index : 0 };
};

impl<T> HashSet:<T> {
	const new := fn (entries : list:<T>) -> HashSet:<T> => return hashset_new(entries);

	const add := fn (self : &mut HashSet:<T>, value : T) -> bool => return hashset_add(self, value);

	const remove := fn (self : &mut HashSet:<T>, value : T) -> bool => return hashset_remove(self, value);

	const contains := fn (self : &HashSet:<T>, value : T) -> bool => return hashset_contains(self, value);

	const len := fn (self : &HashSet:<T>) -> int => return hashset_len(self);

	const is_empty := fn (self : &HashSet:<T>) -> bool => return self.len() = 0;

	const values := fn (self : &HashSet:<T>) -> list:<T> => return hashset_values(self);

	const clear := fn (self : &mut HashSet:<T>) => hashset_clear(self);

	const iter := fn (self : &HashSet:<T>) -> HashSetIter:<T> => return HashSetIter:<T> { data : self.values(), index : 0 };

	const iter_mut := fn (self : &mut HashSet:<T>) -> HashSetIter:<T> => return HashSetIter:<T> { data : self.values(), index : 0 };

	const into_iter := fn (self : HashSet:<T>) -> HashSetIter:<T> => return HashSetIter:<T> { data : self.values(), index : 0 };
};

impl<K, V> Iter for HashMapIter:<K, V> {
	type Item := <K, V>;

	const next := fn (self : &mut Self) -> Self::Item? => {
		if self.index >= len(self.data) => none else => {
			return some(self.data[self.index += 1]);
		};
		return none;	
	};
};

impl<T> Iter for HashSetIter:<T> {
	type Item := T;

	const next := fn (self : &mut Self) -> Self::Item? => {
		if self.index >= len(self.data) => none else => {
			return some(self.data[self.index += 1]);
		};
		return none;
	};
};
