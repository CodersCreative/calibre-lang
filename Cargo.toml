[workspace]
resolver = "2"
members = [
    "calibre",
    "lsp",
    "crates/parser",
    "fmt", "crates/mir", "crates/std", "crates/lir", "crates/vm",
    "crates/frontend",
]

[workspace.package]
version = "0.0.1"
edition = "2024"
publish = false
authors = ["Creative Coders <officialccoders@gmail.com>"]
description = "A high-level c-style language made entirely within rust."
repository = "https://github.com/CodersCreative/calibre-lang"
readme = "README.md"
keywords = ["language", "scripting", "embed"]
categories = ["parsing", "compilers"]
license = "MIT"

[workspace.dependencies]
# Internal packages
calibre-mir = {path="./crates/mir"}
calibre-lir = {path="./crates/lir"}
calibre-vm = {path="./crates/vm"}
calibre-frontend = {path="./crates/frontend"}
calibre-parser = {path="./crates/parser"}
calibre-std = {path="./crates/std"}
calibre-fmt = {path = "./fmt"}

# Other common packages
fastrand = "2"
clap = { version = "4.5.40", features = ["derive"] }
rustc-hash = "2.1.1"
codespan-reporting = "0.13.1"
serde = {version = "1", features = ["derive"]}
thiserror = "2"
rustyline = "17"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt", "json"] }
tracing-flame = "0.2"
tracing-appender = "0.2"
smol = "2.0.2"
derive_builder = "0.20.2"
indextree = {version = "4.9.0", features = ["serde"]}
wasm_thread = "0.3.3"
wasm_sync = "0.1.2"
ustr = {version = "1.1.0", features = ["serde"]}

[workspace.lints.clippy]
# Don't Panic - prevent panics from unwraps and unsafe slicing or indexing
string_slice = "warn"
indexing_slicing = "warn"
unwrap_used = "warn"
panic = "warn"
todo = "warn"
unimplemented = "warn"
unreachable = "warn"
get_unwrap = "warn"
unwrap_in_result = "warn"
unchecked_time_subtraction = "warn"
panic_in_result_fn = "warn"
# Optional - see post for caveats
# expect_used = "warn"
# arithmetic_side_effects = "warn"

# Don't Fail Silently - prevent dropped futures and swallowed errors
let_underscore_future = "warn"
let_underscore_must_use = "warn"
unused_result_ok = "warn"
map_err_ignore = "warn"
assertions_on_result_states = "warn"

# Don't Do Bad Async Stuff - prevent deadlocks and concurrency bugs
await_holding_lock = "warn"
await_holding_refcell_ref = "warn"
if_let_mutex = "warn"  # only relevant on editions before 2024
large_futures = "warn"

# Don't Do Unsafe Things with Memory
mem_forget = "warn"
undocumented_unsafe_blocks = "warn"
multiple_unsafe_ops_per_block = "warn"
unnecessary_safety_doc = "warn"
unnecessary_safety_comment = "warn"

# Don't Do Potentially Incorrect Things with Numbers
float_cmp = "warn"
float_cmp_const = "warn"
lossy_float_literal = "warn"
cast_sign_loss = "warn"
invalid_upcast_comparisons = "warn"
# Optional - these effectively force you to document numeric invariants
# cast_possible_wrap = "warn"
# cast_precision_loss = "warn"
# cast_possible_truncation = "warn"

# Don't Do Bad Things That are Easy to Avoid
rc_mutex = "warn"
debug_assert_with_mut_call = "warn"
iter_not_returning_iterator = "warn"
expl_impl_clone_on_copy = "warn"
infallible_try_from = "warn"
dbg_macro = "warn"

# Don't `allow` Your Way Around These Lints - every suppression must be
# a deliberate #[expect(..., reason = "…")] rather than a silent #[allow]
allow_attributes = "warn"
allow_attributes_without_reason = "warn"
